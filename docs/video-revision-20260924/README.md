# 次回アップデート用・動画改善の作業記録

バージョン1.5はユーザーが提出済み。提出内容・App Store Connect・公開版は変更しない。
ブランチ: `improve-video-learning-modes`。この作業は未完成の試作。pushにはユーザー確認が必要。

## 最新チェックポイント：熱分野4単元

### その後の更新：音声v2・波と音

- 読み仮名の空白調整をNemoの入力前に実施するv2へ移行。完成済みの明示読み27本を音声から再生成し、読みの照合・再描画・公開カタログの更新まで完了。詳細は `../video-fluency-20260924/README.md`。
- 続けて `w-basics` と `w-sound` を登録。本編6本・前提5本を改訂し、専用補足5本（`hw-why-01`〜04、13）を追加。現在のカタログは改訂31本（前提を含む）＋補足18本、合計49本。
- 円の点とサインの投影、逆位相の二つの波と合成ゼロ、管の節・腹、開管の振動数が閉管の2倍である図を修正。補足図は各題材ごとの専用図。音の440Hzを低フレームレートで偽って描かず、440回ぶんを除いた相対的な進みを明示。
- `test-wave-inserts.mjs` で数値例・全字幕状態・SVG/TeXを検査。代表場面を画像で確認し、完成した16本を全デコードしてから登録。音声106文は発音一致を検査し、5文で元の発音を保持する処理を使用。
- 旧374本の全体音声生成は停止済み。旧キャッシュを新しいv2音声として扱わない。今後は `prepare-revision-stage-batch.mjs` で単元ごとに計画を切り出す。
- 波と音のキャッシュ：`/private/tmp/physics-wave-revision`。レンダリング済みでアプリ登録済み。残りの波動（ドップラー・屈折・干渉）と他分野は未完了。
- pushなし。App Store 1.5の提出内容は変更していない。

- `t-heat`、`t-gas`、`t-ideal`、`t-firstlaw` をローカルアプリへ登録。本編12本＋前提7本を改訂し、専用の音声付き補足10本（追補の ht-why-10 を含む）を追加した。
- 既存ガウス試作4本も保持。現在の登録数は改訂20本（前提を含む）＋補足13本。全422本の完成を意味しない。
- 登録時に各MP4を最後までデコードし、H.264/AAC・尺・字幕・読み・原稿一致を確認。不足する補足がある場合はカタログを更新せず停止する。
- 圧力の前提動画の「音声は1Pa、図は10Pa」を修正。反比例の図を滑らかな曲線に変更。潜熱の g / J/g を統一。追加数値例の式を音声の文単位で切り替える。
- Safariで本編→補足→本編の連続再生を確認。動画要素の作り直しが自動再生を妨げていたため、同じ要素で再生するよう修正。再生中のモード切替・再入場のモード保存も確認。後から追加したネイティブシークの区間補正は実ブラウザでの再確認が残る。
- アプリ内ブラウザでは動画再生時にページクラッシュを確認。Safariでは同じ動画が再生できたが、原因の切り分けは未完了。iPhone実機も未確認。
- ビルド、既存25検査、全計画・熱補足図・音声キャッシュの回帰テストを実施。全体の映像／音声の目視・聴取を代替しない。
- 全体音声生成は継続中。残る波動・力学・数学・電磁気の専用映像と新規前提の登録は未完了。push・App Store変更なし。

## 02受領後の作業状況（上記チェックポイント以前）

- `差し替え台本_02_高校熱力学・波動.md` 本体を受領・保存。原稿は9ファイル、字幕／読みは1,115組。追補と本体を区別して取り込み済み。
- ユーザー承認の範囲で、第03弾の2か所の文区切りと、第08弾の「前の差し込み」の参照表現を最小限修正。原稿ファイル自体は変更しない。
- `compile-video-revision.mjs` で既存234本の改訂（タイトルだけの変更を含む）と188本の追加を計画化。合計422本は**制作対象数であり、動画完成数ではない**。
- 字幕のない映像／条件文修正も `compile-report.generated.json` の `visualTasks` に保持。対応先の構造検査が成功しても、映像実装・検証済みとはしない。
- 全体の音声生成を開始。現在の長時間実行はタイトルのみの修正を加える前の374本の計画を読み込んでいるため、完了後に最新422本の計画で再実行が必要。音声が同じものはキャッシュを再利用する。
- 追加した数値例のある `t-firstlaw-intro`、`m-projectile-middle`、`m-momentum-middle` を音声付きMP4として作成し、`/private/tmp/physics-video-full-revision/rendered` に保存。追加場面は専用の図。放物運動は図のラベル重なりと音声・式のタイミングを修正。
- 全範囲カタログの一括登録はしない。完成した単元だけ `register-video-revision-batch.mjs` で登録する。push・1.5の再提出なし。
- `npm run test:video-revision-plan` は全対象ID・差し込み境界・未参照動画・02の18本・追加数値場面を確認する。MP4全件や実機再生の検証を代替しない。
- 既存25検査と新しい計画検査は成功。音声キャッシュの尺保持・選択生成の回帰テストも成功。上記3本のMP4は音声／映像の全デコードと尺の一致を確認。これら3本以外の全件映像検証は未完了。

### 最新計画の再開手順

1. `node scripts/import-video-revision.mjs` → `node scripts/build-video-revision-queue.mjs` → `node scripts/compile-video-revision.mjs`
2. `npm run test:video-revision-plan`
3. 既存の音声生成プロセスの完了を確認してから、`EM_FILM_CACHE=/private/tmp/physics-video-full-revision /private/tmp/physics-video-review-env/bin/python scripts/render-em-film-audio.py`。同じキャッシュで全件生成を並行実行しない。
4. `full-plan.generated.json` の原文指示に合わせた図を実装・確認し、完成メディアだけを登録する。現在の `render-all-films.mjs` は全種類の新規補足に対応したレンダラーではない。
5. 映像修正タスク、追加前提動画、全差し込みルート、全件メディア検査と再生テストが済むまで、全範囲完成とは報告しない。

## 実装・生成済み

- 単元別の「さっと学ぶ／とことん学ぶ」。難易度とは独立して保存。
- 同一MP4の場面区間を参照し、補足終了後に本編へ戻る仕組み。切り替えで本編の時刻を保持。
- ガウスの法則の本編 `ue-gauss-01` 1本を改訂（90.7秒）。字幕は提供台本の場面1・3を使用。ずれの記号はa。
- 補足 `ue-why-08`（42.4秒）、09（33.6秒）、10（47.6秒）を音声付きで生成。本編前に順に挿入。
- 共通の元動画は保存し、試作版カタログで対象1本だけ切り替え。現段階では残りの単元の補足は「準備中」と表示。
- 再生失敗時に「文字とスライドで読む」から既存教材へ戻れる導線。
- 補足欄への最後の一文の機械的な転載を撤去。バトル導線は維持。
- 字幕と読みの分離、音声長による字幕タイミング、語単位の読み補正を生成系へ追加。
- 原稿8ファイルを保存。1,007組の字幕・読みを原文のまま台帳化。
- 既存166本・233場面の変更と、新規169IDの候補を台帳化。ただしこれは生成完了数ではない。
- 1,007組を端末内Nemoで解析し、実際のカナを記録。7種類の頻出語のキーワード照合は警告0。全音声の人による聴取や全語の正しさを保証するものではない。

## 確認

- `npm run build` 成功（既存のバンドルサイズ警告等あり）。最終UI修正後もTypeScript検査成功。
- `node scripts/verify-video-revision.mjs` の25検査成功。結果は `test-results.generated.json`。
- 既存 `test:pronunciation` 成功。
- ブラウザで改訂版タイトル、両モードの切り替え、補足表示、保存したモードによる再入場を確認。
- Safariの連続再生・切替は最新チェックポイント参照。エラー時のスライド遷移とiPhone実機はまだ通し確認していない。純粋関数テストは実機テストの代わりではない。

## 未完了・確認待ち

1. 02本体は受領・取り込み済み。熱の9補足と追補1本を登録済み。波動の9補足と追補は未完了。
2. 第03弾の173行・362行の文区切りは、承認済みの最小修正を生成計画に反映。最終メディア確認が必要。
3. 第08弾 `ue-gauss-03` のモード依存の参照表現は計画に修正済み。最終メディア確認が必要。
4. `prep-coulomb-field` は既存動画の分離・新設が必要。変更台帳は親の `prep-charge-calculation` に誤って上書きしないよう未解決扱い。
5. ガウス本編02以降のa/s表記への改訂、残りの補足、前提割り当ては未反映。現時点の試作は通し学習用の完成版ではない。
6. 他分野の動画差し替え・追加、図の指示の実装、タイトル整備、台本の数式・前後関係の全件点検は未完了。
7. 作業指示5-1のバトル改善、記録、ストアリンクの実機確認等は未着手。1.5を再提出する意図はない。
8. 既存公開音声の全件再生成はしていない。語辞書を直しただけでは公開済みMP4の音声は変わらない。
9. 補足09は指定原稿どおりで約34秒。40秒にするための引き伸ばしや原稿追加はしていない。

## 続きの入口

- `sources/`: 受領原文。変更しない。
- `import-ledger.generated.json`: 全見出し・指示・字幕・読み・原文行番号。
- `revision-queue.generated.json`: 対応先・追加／差し替えの区別・保留事項。追補の追加行は全文置換しない。
- `pilot-plan.json`: 今回の4本の生成用台本。
- `nemo-reading-audit.generated.json`: 実際のカナと検査範囲。
- 生成キャッシュ `/private/tmp/physics-video-mode-pilot`。Nemoはポート50123。
- 取り込み: `node scripts/import-video-revision.mjs` → `node scripts/build-video-revision-queue.mjs`。
- 追加登録: `FFMPEG=… node scripts/register-video-revision-batch.mjs 単元ID`。旧 `register-video-mode-pilot.mjs` は追加済みカタログを4本に戻すため再実行しない。
- 開発サーバーはファイル変更検出が反映されないことがあった。検証ではサーバー再起動とページ再読み込みが必要だった。

新しい台本を独自に書いた場合は、音声化前にユーザー確認。全範囲完了・push済みと報告しない。
