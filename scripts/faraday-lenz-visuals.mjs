import {C,text,line,circle,arrow,path,clamp} from './all-film-visuals.mjs';
import {authoredMotionFrame} from './motion-foundation-visuals.mjs';
const tau=2*Math.PI;
const box=(x,y,w,h,c)=>`<rect x="${x}" y="${y}" width="${w}" height="${h}" rx="12" fill="#101c2a" stroke="${c}" stroke-width="3"/>`;
function coilSide(x=805){return Array.from({length:5},(_,i)=>`<ellipse cx="${x+i*12}" cy="265" rx="57" ry="135" fill="none" stroke="${C.gold}" stroke-width="4"/>`).join('')+text('閉じたコイル',x-30,450,29,C.gold);}
function magnet(x,y){return `<rect x="${x}" y="${y}" width="130" height="80" fill="${C.cyan}"/><rect x="${x+130}" y="${y}" width="130" height="80" fill="${C.red}"/>`+text('S',x+53,y+54,40,'#091120')+text('N',x+180,y+54,40,'#091120');}
function loopArrow(cx,cy,r,p,clockwise,c){const sign=clockwise?1:-1,a0=-Math.PI/2,a=a0+sign*(1.8+2.8*p);return path(Array.from({length:60},(_,i)=>{const b=a0+(a-a0)*i/59;return[cx+r*Math.cos(b),cy+r*Math.sin(b)];}),c,4)+arrow(cx+r*Math.cos(a-sign*.13),cy+r*Math.sin(a-sign*.13),cx+r*Math.cos(a),cy+r*Math.sin(a),c);}
export const faradayLenzKinds=['faraday-approach','faraday-opposing-field','faraday-orientation','faraday-negative-sign','faraday-derivative','faraday-work-budget','faraday-cycle','faraday-kinetic','faradayinsert-change','faradayinsert-turns'];
export function faradayLenzDiagram(kind,p){if(!faradayLenzKinds.includes(kind))throw Error(kind);const u=clamp(p/.7);
 if(kind==='faraday-approach'){const x=180+200*u;return text('磁石のN極を、閉じたコイルへ近づける',180,40,32)+coilSide()+magnet(x,225)+arrow(x+90,160,x+255,160,C.cyan)+text('磁石の速度',x+100,125,28,C.cyan)+arrow(x+235,365,x+60,365,C.purple)+text('磁石が受ける力',x+30,415,28,C.purple)+text('速度と反対の力。コイルは抵抗を通して閉じている',180,505,26);}
 if(kind==='faraday-opposing-field')return text('外からの磁束が増えると、誘導電流はその増加に逆らう',30,40,30)+coilSide(745).replace(text('閉じたコイル',715,450,29,C.gold),'')+magnet(120+120*u,225)+arrow(630,110,1040,110,C.cyan)+text('外からの磁場',750,80,29,C.cyan)+arrow(1040,390,640,390,C.purple)+text('誘導電流が作る磁場',650,465,29,C.purple)+text('外の磁場そのものではなく、磁束の変化に逆らう',180,505,26);
 if(kind==='faraday-orientation')return text('正の法線を手前に決めると、正の周回方向は反時計回り',30,40,30)+`<circle cx="600" cy="275" r="155" fill="none" stroke="${C.dim}" stroke-width="4"/>`+circle(600,275,27,C.gold,.2)+circle(600,275,9,C.gold)+loopArrow(600,275,175,u,false,C.cyan)+text('手前に向く正の法線 ⊙',770,285,28,C.gold)+text('反時計回りが正',105,160,30,C.cyan)+text('法線の側から面を正面に見る。右手の親指が手前、指は反時計回り',40,500,26);
 if(kind==='faraday-negative-sign')return text('手前向きの磁束が増える → 起電力は時計回り',120,40,31)+`<circle cx="560" cy="275" r="160" fill="none" stroke="${C.dim}" stroke-width="4"/>`+[-1,0,1].flatMap(i=>[-1,0,1].map(j=>circle(560+i*75,275+j*75,4+6*u,C.cyan))).join('')+loopArrow(560,275,180,u,true,C.purple)+text('青い点：手前向きの磁場',755,160,26,C.cyan)+text('時計回りは負の向き',750,390,29,C.purple)+text('正の磁束が増える場合。正の周回方向は反時計回り',170,505,27);
 if(kind==='faraday-derivative'){const dt=.85-.73*u,a=1,b=a+dt,X=t=>180+400*t,Y=f=>425-8500*f,phi=t=>.01*t*t,ya=phi(a),yb=phi(b),m=(yb-ya)/dt;return text('二点を結ぶ傾きから、一点での接線の傾きへ',105,35,31)+line(180,425,1090,425,C.dim,3)+line(180,425,180,90,C.dim,3)+path(Array.from({length:101},(_,i)=>{const t=1.9*i/100;return[X(t),Y(phi(t))];}),C.cyan,4)+line(X(.7),Y(ya+m*(.7-a)),X(1.9),Y(ya+m*(1.9-a)),C.gold,3)+line(X(.7),Y(ya+.02*(.7-a)),X(1.9),Y(ya+.02*(1.9-a)),C.purple,3,'6 7')+circle(X(a),Y(ya),9,C.ink)+circle(X(b),Y(yb),9,C.gold)+line(X(a),Y(ya),X(b),Y(ya),C.gold,2,'5 6')+line(X(b),Y(ya),X(b),Y(yb),C.gold,2,'5 6')+text('磁束 [Wb]',95,80,28)+text('時間 [s]',970,465,28)+text(`時間幅 Δt=${dt.toFixed(2)} s`,260,110,28,C.gold)+text('紫の破線：1秒の点の接線',650,95,27,C.purple)+text('横の差はΔt、縦の差はΔΦ。比が磁束の変化の速さ',120,505,27);}
 if(kind==='faraday-work-budget')return text('同じ速さで動かすと、人の仕事が電気エネルギーになる',30,40,30)+[0,1,2].map(i=>{const x=80+i*395;return box(x,170,300,220,[C.cyan,C.purple,C.gold][i])+text(['人がする仕事','電気エネルギー','抵抗の熱'][i],x+35,285,29);}).join('')+[380,775].map(x=>arrow(x+5,275,x+85,275,C.gold)).join('')+circle(410+400*u,135,9,C.gold)+text('一定速度の比較。一連の操作後に磁石とコイルの状態を戻して数える',30,500,26);
 if(kind==='faraday-cycle')return text('元の配置・状態へ戻す操作全体で、エネルギーを数える',40,40,30)+box(180,170,310,220,C.cyan)+box(740,170,310,220,C.purple)+text('出発時の状態',235,280,30,C.cyan)+text('同じ状態へ戻す',780,280,29,C.purple)+arrow(505,225,720,225,C.gold)+arrow(720,350,505,350,C.gold)+circle(510+195*u,225,10,C.gold)+text('操作する',555,185,28)+text('元へ戻す',555,410,28)+text('取り出した熱を外からの仕事で補わなければ、保存則に反する',70,500,27);
 if(kind==='faraday-kinetic'){const x=135+300*(1-(1-u)**2),v=180*(1-u)+25;return text('外から押さない場合は、磁石の運動エネルギーが減る',40,40,30)+coilSide(860)+magnet(x,220)+arrow(x+90,145,x+90+v,145,C.cyan)+text('速度の矢印が短くなる',x+30,105,28,C.cyan)+text('減った運動エネルギーが、取り出す電気エネルギーを補う',70,505,27);}
 if(kind==='faradayinsert-change')return text('一巻きの磁束が、0.1秒間に0.01 Wb変わる',155,40,31)+line(200,410,1000,410,C.dim,3)+line(200,410,200,115,C.dim,3)+line(200,410,920,150,C.cyan,4)+circle(200+720*u,410-260*u,11,C.gold)+line(920,150,920,410,C.gold,2,'6 7')+text('磁束 [Wb]',105,95,28)+text('0.01',115,160,29)+text('0',165,450,28)+text('0.1 s',885,450,28)+text('時間',1020,420,27)+text('この区間の平均を求める。向きではなく起電力の大きさを計算',50,505,26);
 if(kind==='faradayinsert-turns'){const count=Math.floor(100*u);return text('一巻き0.1 Vを、同じ向きに100巻き分足す',145,40,31)+Array.from({length:100},(_,i)=>`<rect x="${180+i%10*62}" y="${100+Math.floor(i/10)*34}" width="48" height="23" rx="4" fill="${i<count?C.gold:C.dim}" opacity="${i<count?.9:.15}"/>`).join('')+text('1マスが一巻き',825,165,28)+text('各0.1 V',845,225,31,C.gold)+text(`${count}巻き`,845,305,32)+text(`合計 ${(count*.1).toFixed(1)} V`,825,385,32,C.gold)+text('各巻きに同じ磁束の変化。同じ向きの起電力が直列に加わる',75,495,26);}
}
export function faradayLenzFrame(c,s,t){return c.visualPilot==='faraday-lenz-v1'?authoredMotionFrame(c,s,t,faradayLenzDiagram):null;}
