import {C,text,line,circle,arrow,path,clamp} from './all-film-visuals.mjs';
import {authoredMotionFrame} from './motion-foundation-visuals.mjs';
const metal=(x,y,w=330)=>`<rect x="${x}" y="${y}" width="${w}" height="95" rx="12" fill="${C.dim}" opacity=".28"/>`+text('金属',x+w/2-30,y+65,28);
const packet=(x,y,c=C.green)=>path(Array.from({length:61},(_,i)=>[x-35+i*70/60,y+14*Math.sin(i*Math.PI/10)*Math.sin(i*Math.PI/60)]),c,4);
const electron=(x,y)=>circle(x,y,13,C.cyan)+text('−',x-8,y+6,21,'#091120');
export const photonFoundationKinds=['photon-barrier','photon-absorption','photon-brightness','photon-threshold','photon-material','photon-green','photon-ev'];
export function photonFoundationDiagram(kind,p){if(!photonFoundationKinds.includes(kind))throw Error(kind);const u=clamp(p/.8);
 if(kind==='photon-barrier')return text('電子を金属の外へ出すには、エネルギーが要る',135,40,31)+metal(260,325,670)+electron(400,350-180*u)+arrow(455,330,455,150,C.gold)+text('外へ出すためのエネルギー',490,220,30,C.gold)+text('外',320,130,29)+text('電子',315,315-150*u,27,C.cyan)+text('位置の模式図。電子の大きさ・距離は実際の縮尺ではない',170,495,25);
 if(kind==='photon-absorption'){const q=clamp(u/.5),r=clamp((u-.5)/.5);return text('光子一個が吸収され、電子へエネルギーが渡る',120,40,31)+metal(380,330,510)+(u<.5?packet(230+300*q,130+200*q):'')+electron(530+200*r,340-210*r)+arrow(710,250,835,125,C.cyan)+text('光子',175,95,28,C.green)+text('飛び出す電子',815,110,28,C.cyan)+text('この図は、一個で十分なエネルギーを渡せる場合',210,495,26);}
 if(kind==='photon-brightness'){let out=text('同じ色の光：一個は同じ。明るい方は個数が多い',95,40,31);for(let j=0;j<2;j++){const y=190+j*190,n=j?7:3;out+=text(j?'明るい光':'暗い光',100,y-65,30,C.green);for(let i=0;i<n;i++)out+=packet(290+(i*105+u*90)%735,y);out+=line(1070,y-55,1070,y+45,C.dim,4);}return out+text('同じ時間・同じ面積に届く光を比較。波形は光子の記号',155,500,26);}
 if(kind==='photon-threshold'){let out=text('同じ金属に当てる光の、振動数だけを変える',120,40,31);for(let j=0;j<2;j++){const x=140+j*570,h=j?220:100;out+=text(j?'一個で足りる光':'一個では足りない光',x,105,28,j?C.purple:C.red)+`<rect x="${x+130}" y="${400-h}" width="90" height="${h}" fill="${j?C.purple:C.red}" opacity="${.6*u}"/>`+line(x+40,235,x+330,235,C.gold,3,'7 5')+text('必要な量',x+230,220,25,C.gold)+text(j?'電子が出る':'電子は出ない',x+70,450,29,C.cyan);}return out+text('棒の高さ：光子一個のエネルギー。位置の高さではない',155,505,25);}
 if(kind==='photon-material'){let out=text('同じ光でも、必要なエネルギーが違えば結果は違う',85,40,31);for(let j=0;j<2;j++){const x=155+j*550,y=j?180:300;out+=text('金属'+(j?'B':'A'),x+95,115,32)+`<rect x="${x+105}" y="${415-180}" width="85" height="180" fill="${C.red}" opacity="${.55*u}"/>`+line(x+20,y,x+335,y,C.gold,3,'6 5')+text('必要な量',x+245,y-12,24,C.gold)+text(j?'電子は出ない':'電子が出る',x+65,465,29,C.cyan);}return out+text('光の色だけでは判定できない。金属の種類も必要',220,510,25);}
 if(kind==='photon-green'){const pts=Array.from({length:301},(_,i)=>[180+3*i,275+70*Math.sin(i/300*Math.PI*12-u*Math.PI*2)]);return text('緑の光の一例：振動数 5.5×10¹⁴ Hz',195,45,33)+path(pts,C.green,5)+text('一秒あたりの振動回数を表す',350,125,30)+text('1の後に0が14個：100,000,000,000,000',220,440,29)+text('画面の振動は観察用に大幅に遅くしている',300,500,26);}
 if(kind==='photon-ev'){return text('電子ボルトは、電子が得るエネルギーの単位',140,40,31)+line(265,140,265,400,C.purple,7)+line(965,140,965,400,C.gold,7)+text('低い電位',175,105,29,C.purple)+text('1 V高い電位',855,105,29,C.gold)+electron(320+570*u*u,270)+arrow(385,330,850,330,C.cyan)+text('電子は負の電荷なので、高い電位へ加速する',250,455,28)+text('得るエネルギーは 1 eV（電子ボルト）',315,505,28,C.gold);}
}
export function photonFoundationFrame(c,s,t){return c.visualPilot==='photon-foundations-v1'?authoredMotionFrame(c,s,t,photonFoundationDiagram):null;}
