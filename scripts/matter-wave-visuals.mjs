import {C,text,line,circle,arrow,path,clamp} from './all-film-visuals.mjs';
import {authoredMotionFrame} from './motion-foundation-visuals.mjs';
const fract=x=>x-Math.floor(x),rnd=i=>fract(Math.sin(i*127.1+311.7)*43758.5453);
const density=x=>Math.exp(-x*x/1.1)*Math.cos(x*Math.PI*3)**2;
const points=[];for(let i=1;points.length<440;i++){const x=rnd(i*3)*2-1;if(rnd(i*3+1)<density(x))points.push([600+x*400,170+rnd(i*3+2)*220]);}
const wave=(x,y,w,n,p,c)=>path(Array.from({length:121},(_,i)=>[x+w*i/120,y+35*Math.sin(i/120*Math.PI*2*n-p*Math.PI*2)]),c,3);
export const matterWaveKinds=['matter-apparatus','matter-dots','matter-distribution','matter-not-water','matter-wavelength','matterinsert-accelerate','matterinsert-crystal'];
export function matterWaveDiagram(kind,p){if(!matterWaveKinds.includes(kind))throw Error(kind);const u=clamp(p/.8);
 if(kind==='matter-apparatus')return text('一個ずつ送った電子を、スクリーンで記録する',120,40,31)+circle(160,265,24,C.cyan)+text('電子源',110,340,29)+line(530,105,530,205,C.dim,8)+line(530,235,530,300,C.dim,8)+line(530,330,530,425,C.dim,8)+text('二つの隙間',440,475,30)+line(1020,100,1020,425,C.gold,7)+text('スクリーン',920,475,28,C.gold)+circle(u<.4?200+270*u/.4:1020,u<.4?265:235,10,C.cyan)+text('途中の経路を断定する図ではない',205,105,27);
 if(kind==='matter-dots'){const n=Math.max(1,Math.floor(440*u));return text('正面から見たスクリーン：一点ずつの記録が、縞になる',45,40,30)+`<rect x="160" y="130" width="880" height="300" fill="#02050c"/>`+points.slice(0,n).map(([x,y])=>circle(x,y,3,C.cyan,.8)).join('')+text(`記録を重ねる：${n}個`,460,490,30,C.cyan)+text('干渉分布を用いた模式的なシミュレーション',300,100,26);}
 if(kind==='matter-distribution')return text('波の性質が現れるのは、届きやすさの分布',160,40,31)+line(185,395,1040,395,C.dim,3)+line(200,410,200,125,C.dim,3)+text('届きやすさ',205,100,29,C.cyan)+path(Array.from({length:301},(_,i)=>{const x=-1+2*i/300;return[600+x*400,395-235*density(x)];}),C.cyan,4)+circle(200+800*u,395-235*density(-1+2*u),9,C.gold)+text('スクリーン上の位置',770,450,29)+text('山：点が多い場所。谷：点が少ない場所',325,505,28);
 if(kind==='matter-not-water')return text('一点の記録と、記録の分布は、別の見方',175,40,32)+`<rect x="145" y="135" width="350" height="255" fill="#02050c"/><rect x="610" y="135" width="440" height="255" fill="#02050c"/>`+circle(320,255,7+2*u,C.cyan)+points.slice(0,200).map(([x,y])=>circle(830+(x-600)*.45,140+(y-170),2,C.cyan,.75)).join('')+text('一回：一つの点',200,450,30,C.cyan)+text('多数：届きやすい場所の縞',630,450,29,C.cyan)+text('電子が水面の波になる、という図ではない',325,510,26);
 if(kind==='matter-wavelength'){let out=text('同じ電子：運動量が大きいほど、対応する波長は短い',65,40,31);for(let j=0;j<2;j++){const y=180+j*205;out+=text(j?'運動量が2倍':'ある運動量',65,y-60,29)+wave(330,y,700,j?8:4,u,j?C.purple:C.cyan)+line(340,y+70,340+(j?87.5:175),y+70,C.gold,4)+text(j?'波長は半分':'波長',560,y+85,29,C.gold);}return out+text('曲線は波長の比較用。電子の軌道や形ではない',305,505,25);}
 if(kind==='matterinsert-accelerate')return text('電子を100 Vの電位差で加速してから、波長を求める',65,40,31)+line(210,135,210,400,C.purple,7)+line(950,135,950,400,C.gold,7)+text('低い電位',135,100,29,C.purple)+text('100 V高い電位',825,100,29,C.gold)+circle(260+620*u*u,270,13,C.cyan)+arrow(360,350,780,350,C.cyan)+text('初めはほぼ静止 → 運動エネルギーを得る',275,480,29);
 if(kind==='matterinsert-crystal'){let out=text('原子の規則的な並びで散乱した波が、互いに干渉する',45,40,31);for(let i=0;i<5;i++)for(let j=0;j<4;j++)out+=circle(360+120*i,145+80*j,14,C.gold,.8);out+=wave(80,275,190,2.2,u,C.cyan)+arrow(915,255,1080,150,C.purple)+arrow(915,295,1080,395,C.purple)+line(360,455,480,455,C.gold,3)+text('原子どうしの間隔',310,510,28,C.gold)+text('電子の波長：約0.12 nm',655,505,29,C.cyan);return out;}
}
export function matterWaveFrame(c,s,t){return c.visualPilot==='matter-waves-v1'?authoredMotionFrame(c,s,t,matterWaveDiagram):null;}
