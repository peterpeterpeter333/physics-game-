import {C,text,line,circle,arrow,path,clamp} from './all-film-visuals.mjs';
import {authoredMotionFrame} from './motion-foundation-visuals.mjs';
const rect=(x,y,w,h,c)=>`<rect x="${x}" y="${y}" width="${w}" height="${h}" fill="#101a29" stroke="${c}" stroke-width="3"/>`;
const resistor=(x,y,label,c=C.gold)=>rect(x,y-25,150,50,c)+text(label,x+30,y+10,30,c);
function series(p,numeric=false){const u=clamp(p/.7);return path([[170,280],[170,170],[1010,170],[1010,410],[170,410],[170,310]],C.dim,4)+line(125,280,215,280,C.purple,5)+line(145,310,195,310,C.purple,5)+text(numeric?'12 V':'電池',55,235,29,C.purple)+resistor(360,170,numeric?'2 Ω':'R₁',C.cyan)+resistor(700,170,numeric?'4 Ω':'R₂',C.gold)+[260,590,900].map(x=>arrow(x-40,95,x+35,95,C.cyan)+text(numeric?'2 A':'同じ I',x-33,65,26,C.cyan)).join('')+circle(220+750*u,170,11,C.cyan)+text(numeric?'4 V':'電圧 V₁',370,270,29,C.cyan)+text(numeric?'8 V':'電圧 V₂',710,270,29,C.gold)+text('導線の抵抗と電池の内部抵抗は無視',295,485,27);}
function parallel(p,numeric=false){const u=clamp(p/.7),left=250,right=1050;return line(120,280,left,280,C.dim,4)+line(left,150,left,400,C.cyan,4)+line(right,150,right,400,C.purple,4)+line(left,150,right,150,C.dim,4)+line(left,400,right,400,C.dim,4)+line(right,280,1160,280,C.dim,4)+resistor(560,150,numeric?'2 Ω':'R₁',C.cyan)+resistor(560,400,numeric?'4 Ω':'R₂',C.gold)+text('点A',205,100,29,C.cyan)+text('点B',1015,100,29,C.purple)+arrow(120,240,220,240,C.gold)+text(numeric?'全体 9 A':'全体 I',55,205,27,C.gold)+[150,400].map((y,i)=>arrow(340,y-55,470,y-55,i?C.gold:C.cyan)+text(numeric?(i?'3 A':'6 A'):(i?'枝の I₂':'枝の I₁'),350,y-80,27,i?C.gold:C.cyan)+circle(290+700*((u*(i?1:2))%1),y,10,i?C.gold:C.cyan)).join('')+text(numeric?'どちらも点Aから点Bまで12 V':'どちらも同じ点Aと点Bを結ぶ → 同じ電圧V',320,285,27)+text('A・Bは電池の両端につながる。理想的な導線を扱う',180,505,26);}
export const resistorCircuitKinds=['resistor-ohm-two-points','resistor-lamp-curve','resistor-series-flow','resistor-series-drops','resistor-parallel-flow','resistor-common-quantity','resistorinsert-series','resistorinsert-parallel'];
export function resistorCircuitDiagram(kind,p){if(!resistorCircuitKinds.includes(kind))throw Error(kind);const u=clamp(p/.7);
 if(kind==='resistor-ohm-two-points')return text('3 Ωの抵抗：電圧を二倍にした二点を比べる',140,35,32)+line(220,425,1070,425,C.dim,3)+line(220,425,220,110,C.dim,3)+line(220,425,980,145,C.cyan,4)+[0,1].map(i=>{const x=600+i*380,y=285-i*140;return circle(x,y,10,i?C.gold:C.cyan)+line(x,y,x,425,C.dim,2,'6 6')+text(i?'12 V・4 A':'6 V・2 A',x-115,y-35,29,i?C.gold:C.cyan)+text(i?'12':'6',x-15,460,25);}).join('')+circle(220+760*u,425-280*u,7,C.gold)+text('電圧 [V]',970,485,27)+text('電流 [A]',140,90,27)+text('原点を通る直線。温度を一定に保つ',290,505,26);
 if(kind==='resistor-lamp-curve')return text('温度が変わる電球では、電圧と電流は比例しない',90,35,31)+line(220,425,1080,425,C.dim,3)+line(220,425,220,110,C.dim,3)+line(220,425,1000,135,C.cyan,3,'7 8')+path(Array.from({length:121},(_,i)=>{const x=i/120;return[220+780*x,425-290*x/(1+1.7*x)];}),C.gold,4)+circle(220+780*u,425-290*u/(1+1.7*u),10,C.gold)+text('温度一定の抵抗',670,120,28,C.cyan)+text('熱くなる電球',735,280,29,C.gold)+text('電流 [A]',130,95,27)+text('電圧 [V]',965,470,27)+text('性質の違いを示す模式的なグラフ。実測値ではない',160,505,26);
 if(kind==='resistor-series-flow')return text('直列：分かれ道がなく、同じ電流が順に流れる',110,30,30)+series(p);
 if(kind==='resistor-series-drops'){const points=[[160,130],[370,130],[370,230],[730,230],[730,430],[1040,430]],s=clamp(p/.7)*1180;let d=s,q=points.at(-1);for(let i=1;i<points.length;i++){const a=points[i-1],b=points[i],len=Math.hypot(b[0]-a[0],b[1]-a[1]);if(d<=len){q=[a[0]+(b[0]-a[0])*d/len,a[1]+(b[1]-a[1])*d/len];break;}d-=len;}return text('電池で得たエネルギーを、二つの抵抗に分けて渡す',65,35,31)+path(points,C.dim,4)+text('電池の＋側',140,90,28,C.purple)+text('電池の−側',890,470,28,C.purple)+arrow(410,145,410,215,C.cyan)+text('一つ目で下がる分',445,185,28,C.cyan)+arrow(770,245,770,415,C.gold)+text('二つ目で下がる分',805,325,28,C.gold)+line(90,130,90,430,C.purple,3)+text('全体の差',15,310,25,C.purple)+circle(q[0],q[1],11,C.gold)+text('高さは1 Cあたりのエネルギー。実際の導線の高さではない',90,505,26);}
 if(kind==='resistor-parallel-flow')return text('並列：同じ二点の間に、二つの電流の通り道がある',90,35,30)+parallel(p);
 if(kind==='resistor-common-quantity')return text('どの量が共通かを、式を作る前に確かめる',125,35,32)+[0,1].map(i=>{const x=100+600*i;return text(i?'並列':'直列',x+150,100,33)+rect(x,135,430,290,C.dim)+text(i?'電圧が共通':'電流が共通',x+105,215,34,i?C.purple:C.cyan)+text(i?'枝の電流を足す':'各抵抗の電圧を足す',x+70,335,29,C.gold)+arrow(x+215,240,x+215,270+30*u,C.gold);}).join('')+text('共通の量が違うため、抵抗の足し方も違う',230,495,29);
 if(kind==='resistorinsert-series')return text('2 Ωと4 Ωを直列に：同じ2 Aが流れる',160,30,30)+series(p,true);
 if(kind==='resistorinsert-parallel')return text('2 Ωと4 Ωを並列に：各枝の電流を合計する',130,35,30)+parallel(p,true);
}
export function resistorCircuitFrame(c,s,t){return c.visualPilot==='resistor-circuits-v1'?authoredMotionFrame(c,s,t,resistorCircuitDiagram):null;}
