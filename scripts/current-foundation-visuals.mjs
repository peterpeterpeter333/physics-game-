import {C,text,line,circle,arrow,path,clamp} from './all-film-visuals.mjs';
import {authoredMotionFrame} from './motion-foundation-visuals.mjs';
const box=(x,y,w,h,c)=>`<rect x="${x}" y="${y}" width="${w}" height="${h}" rx="16" fill="none" stroke="${c}" stroke-width="3"/>`;
const charge=(x,y,sign='+',c=C.cyan)=>circle(x,y,17,c,.2)+text(sign,x-9,y+10,28,c);
const dotStream=(p,y,sign='+',count=12)=>Array.from({length:count},(_,i)=>charge(150+900*(((i/count+p*.35)%1+1)%1),y,sign)).join('');
const bar=(x,y,w,h,c)=>`<rect x="${x}" y="${y-h}" width="${w}" height="${h}" fill="${c}" rx="5"/>`;
export const currentFoundationKinds=['currentprep-forces','currentprep-charge-unit','currentprep-gate','currentprep-direction','currentprep-voltage','currentprep-ohm','currentintro-two-gates','currentintro-steady','currentintro-energy','currentintro-per-charge','currentintro-compare-voltage','currentinsert-accumulation','currentinsert-balanced'];
export function currentFoundationDiagram(kind,p){
 if(!currentFoundationKinds.includes(kind))throw Error(kind);const u=clamp(p/.7);
 if(kind==='currentprep-forces')return text('電荷の符号で、受ける力の向きが変わる',170,40,32)+[0,1].map(i=>{const y=185+210*i,x1=380,x2=790,sgn=i?1:-1;return charge(x1,y,'+')+charge(x2,y,i?'−':'+',C.purple)+arrow(x1,y,x1+sgn*(85+25*u),y,C.gold)+arrow(x2,y,x2-sgn*(85+25*u),y,C.gold)+text(i?'違う符号：引き合う':'同じ符号：反発する',420,y-75,29)+text('力',x1+sgn*100-15,y+48,27,C.gold);}).join('')+text('矢印は、それぞれの電荷が受ける力',290,505,28);
 if(kind==='currentprep-charge-unit')return text('電荷の量は、粒の個数とは別の量',200,40,32)+box(190,110,810,275,C.dim)+Array.from({length:30},(_,i)=>charge(235+i%10*78+6*Math.sin(p*6+i),155+Math.floor(i/10)*85,'−',C.purple)).join('')+text('一つ一つの電子は、マイナスの電荷を持つ',275,440,29,C.purple)+text('1 Cは電子1個ではない。電子1個の電荷量は約−1.6×10⁻¹⁹ C',70,505,26);
 if(kind==='currentprep-gate')return text('同じ断面を通った電荷を、時間とともに数える',100,40,32)+line(130,175,1070,175,C.dim,3)+line(130,315,1070,315,C.dim,3)+dotStream(p,245)+line(600,135,600,350,C.gold,5)+text('数える断面',535,110,29,C.gold)+text(`経過時間 ${(2*u).toFixed(1)} s`,200,420,31)+text(`通過した電荷 ${(4*u).toFixed(1)} C`,690,420,31,C.cyan)+text('粒は電荷の流れの模式図。粒1個が1 Cという意味ではない',125,505,26);
 if(kind==='currentprep-direction')return text('金属中では、電子の移動と電流の向きは逆',125,40,31)+line(140,200,1060,200,C.dim,3)+line(140,340,1060,340,C.dim,3)+dotStream(-p,270,'−')+arrow(310,145,910,145,C.gold)+text('電流の向き',490,100,31,C.gold)+arrow(910,400,310,400,C.purple)+text('電子の移動する向き',420,460,31,C.purple)+text('電流の正方向は、正の電荷が進む向きとして定義する',160,505,26);
 if(kind==='currentprep-voltage')return text('同じ3 Vの抵抗：通る電荷が二倍なら、渡すエネルギーも二倍',35,40,29)+[0,1].map(i=>{const x=320+500*i,h=(i+1)*110*u;return bar(x,395,150,h,C.gold)+text(`${i+1} Cが通る`,x-5,445,30,C.cyan)+text(`${3*(i+1)} Jを渡す`,x-10,110,30,C.gold);}).join('')+line(220,395,1060,395,C.dim,3)+text('縦の棒はエネルギー。電荷が減った量を表す棒ではない',125,505,27);
 if(kind==='currentprep-ohm'){const v=6*u,i=v/2;return text('温度一定の抵抗：電圧と電流は比例する',160,40,32)+line(250,410,1050,410,C.dim,3)+line(250,410,250,100,C.dim,3)+line(250,410,970,140,C.cyan,4)+circle(250+120*v,410-90*i,11,C.gold)+line(250+120*v,410,250+120*v,410-90*i,C.gold,2,'6 7')+text('電圧 [V]',930,480,29)+text('電流 [A]',140,95,29)+[0,2,4,6].map(n=>text(String(n),242+120*n,447,24)).join('')+[1,2,3].map(n=>text(String(n),209,418-90*n,24)).join('')+text(`電圧 ${v.toFixed(1)} V → 電流 ${i.toFixed(1)} A`,420,110,30,C.gold)+text('2 Ωの抵抗を例にする。すべての部品に成り立つ関係ではない',90,505,26);}
 if(kind==='currentintro-two-gates')return text('電球の前と後ろを、同じ1秒間で比べる',160,40,32)+line(100,255,1100,255,C.dim,5)+dotStream(p,255)+circle(600,255,65,C.gold,.25)+text('電球',565,205,29,C.gold)+[350,850].map((x,i)=>line(x,150,x,365,i?C.purple:C.cyan,5)+text(i?'出口の断面':'入口の断面',x-75,110,28,i?C.purple:C.cyan)+text(`${(2*u).toFixed(1)} C`,x-45,420,31,i?C.purple:C.cyan)).join('')+text(`計測時間 ${(u).toFixed(1)} s。電荷の粒は流れの模式図`,240,505,27);
 if(kind==='currentintro-steady')return text('定常状態：中にたまった電荷の量が変わらない状態',75,40,31)+box(420,145,360,220,C.gold)+text('電球の中',520,200,31,C.gold)+text('電荷の増加はゼロ',475,295,30)+arrow(130,255,405,255,C.cyan)+arrow(795,255,1070,255,C.purple)+text('入る量',185,190,29,C.cyan)+text('出る量',865,190,29,C.purple)+circle(140+240*u,255,12,C.cyan)+circle(810+240*u,255,12,C.purple)+text('入った量と同じ量が出る。電球を通っても電流は減らない',105,470,28)+text('外との出入りを含めて、電荷の合計を数える',265,505,26);
 if(kind==='currentintro-energy'){const points=[[250,360],[250,180],[920,180],[920,360],[250,360]],s=4*p%4,i=Math.floor(s),a=points[i],b=points[i+1],x=a[0]+(b[0]-a[0])*(s-i),y=a[1]+(b[1]-a[1])*(s-i);return text('電荷の流れと、エネルギーの変換を分けて見る',110,40,31)+path(points,C.cyan,4)+box(175,235,150,80,C.purple)+text('電池',214,285,29,C.purple)+circle(920,270,60,C.gold,.25)+text('電球',885,280,29,C.gold)+charge(x,y)+arrow(985,225,1100,135,C.gold)+arrow(985,315,1100,405,C.gold)+text('光',1100,100,29,C.gold)+text('熱',1100,450,29,C.gold)+text('化学エネルギーを使う',55,445,27,C.purple)+text('青：電荷の循環の模式図。金：電球から出るエネルギー',120,505,27);}
 if(kind==='currentintro-per-charge')return text('3 V：プラス1 Cあたり、3 Jのエネルギー差',180,40,31)+line(180,160,480,160,C.cyan,4)+line(700,385,1050,385,C.purple,4)+text('入口',270,120,30,C.cyan)+text('出口',835,440,30,C.purple)+arrow(455,175,720,370,C.gold)+charge(455+265*u,175+195*u)+text('抵抗に3 Jを渡す',640,180,31,C.gold)+text('高さはエネルギーの比較。導線を坂道に曲げる図ではない',110,505,27);
 if(kind==='currentintro-compare-voltage')return text('同じ1 Cが通る場合でも、渡すエネルギーは違う',80,40,31)+[0,1].map(i=>{const x=260+510*i;return box(x-50,125,285,290,i?C.purple:C.cyan)+text(`${i?6:3} V`,x+35,180,36)+charge(x+20+100*u,250)+arrow(x+70,285,x+70,320+(i?65:25),C.gold)+text(`${i?6:3} Jを渡す`,x+5,460,29,C.gold);}).join('')+text('電流は通過量のペース、電圧は1 Cあたりのエネルギー差',100,505,26);
 if(kind==='currentinsert-accumulation')return text('仮定：毎秒2 C入るのに、毎秒1 Cしか出ない',135,40,31)+box(440,130,330,260,C.gold)+arrow(130,250,425,250,C.cyan)+arrow(785,250,1080,250,C.purple)+text('入口 2 C/s',165,180,30,C.cyan)+text('出口 1 C/s',825,180,30,C.purple)+bar(550,350,110,160*u,C.gold)+text(`中の増加 ${(3*u).toFixed(1)} C`,455,440,29,C.gold)+text(`経過時間 ${(3*u).toFixed(1)} s`,470,100,28)+text('この仮定では電荷が増え続けるので、定常状態ではない',120,505,27);
 if(kind==='currentinsert-balanced')return text('定常状態：1秒間に入る量と出る量をそろえる',95,40,31)+box(450,140,310,240,C.dim)+arrow(140,250,430,250,C.cyan)+arrow(780,250,1070,250,C.purple)+text('入口 2 C/s',165,180,30,C.cyan)+text('出口 2 C/s',830,180,30,C.purple)+text('電荷の増加',505,220,29)+text('0 C',565,300,39,C.gold)+[0,1].map(i=>circle((i?800:150)+230*u,250,13,i?C.purple:C.cyan)).join('')+text('電荷の量は増えない。電球はエネルギーを光や熱に変える',80,485,28);
}
export function currentFoundationFrame(c,s,t){return c.visualPilot==='current-foundations-v1'?authoredMotionFrame(c,s,t,currentFoundationDiagram):null;}
